package com.xephyrka.liora

import org.junit.Test

import org.junit.Assert.*

/**
 * Example local unit test class that executes on the development machine (JVM).
 * Used for testing pure logic that does not depend on the Android framework.
 */
class ExampleUnitTest {
    /** 
     * Simple sanity check verifying that basic arithmetic operations behave as expected.
     */
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
